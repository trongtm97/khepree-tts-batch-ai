# Optional: Spark-TTS 0.5B (SparkAudio).
# Installed into userData/runtimes/spark/site-packages — NEVER core bundle.
# No Gradio. No Conda required for end-user (pip --target only).
#
# Upstream also vendors sparktts/ + cli/ via shallow git clone into runtime upstream/.
#
einops>=0.8.0
einx>=0.3.0
numpy>=1.26
omegaconf>=2.3.0
packaging>=24.0
safetensors>=0.4.0
soundfile>=0.12.0
soxr>=0.5.0
torch>=2.1.0
torchaudio>=2.1.0
tqdm>=4.66.0
transformers>=4.46.0
huggingface_hub>=0.24.0
# intentionally omit: gradio
